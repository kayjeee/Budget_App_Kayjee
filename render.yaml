databases:
  - name: budget_app_Kayjeee
    databaseName: budgetapp_0tun
    user: kagis
    ipAllowList: []

services:
  - type: web
    name: rails
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kagis
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
  